# Commands

```powershell
python detect_baseline_singles_cv2.py `
  --input "path\to\image.jpg" `
  --out-dir "line_detection_output"
```

```powershell
$env:ROBOFLOW_API_KEY="your_api_key"

python detect_baseline_singles_cv2.py `
  --input "path\to\image.jpg" `
  --out-dir "line_detection_output" `
  --use-roboflow-roi
```
